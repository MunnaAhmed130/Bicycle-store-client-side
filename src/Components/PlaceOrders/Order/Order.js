import React from 'react';
import { Button, Card, Col } from 'react-bootstrap';

const Order = ({ order }) => {
    const { name, url, prize } = order;
    return (
        <div>

        </div>
    );
};

export default Order;